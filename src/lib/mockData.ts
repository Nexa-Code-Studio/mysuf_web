import type { UserRole } from "@/types";

export const mockUser: { name: string; role: UserRole; initials: string } = {
  name: "Budi Santoso",
  role: "GOV_ADMIN",
  initials: "BS",
};

export const mockNotificationCount = 3;
